# CLAUDE.md

## Git

This is a personal project, so the global "commit, push or branch only when I ask" rule does not apply here. Branch, commit as you go, and push finished work without checking in first. Still stop and ask before anything that rewrites shared history — force pushes, resets on pushed branches, tag deletion.

No Co-Authored-By lines in commits.
